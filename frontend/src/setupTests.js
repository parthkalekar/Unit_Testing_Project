// frontend/src/setupTests.js
import '@testing-library/jest-dom';

// optional: polyfills or globals for tests
// e.g. make fetch available in node environment if needed
// global.fetch = global.fetch || (() => Promise.reject(new Error('no fetch polyfill')));
